paths:
    migrations: %%PHINX_CONFIG_DIR%%/database/migrations

environments:
    default_migration_table: phinxlog
    default_database: minion

    development:
        adapter: mysql
        host: localhost
        name: minion
        user: root
        pass: 
        port: 3306
        charset: utf8

    production:
        adapter: mysql
        host: localhost
        name: minion
        user: root
        pass: 
        port: 3306
        charset: utf8